# CAS — Nginx config (production)
#
# THIS FILE IS THE SOURCE OF TRUTH: /etc/nginx/sites-available/cas.conf
# /etc/nginx/sites-enabled/cas.conf must be a SYMLINK to it. It was a separate
# regular file until 2026-08-19, which let the two drift 4.5 months apart --
# the sites-available copy had no /api/v2/ block at all, so anyone who
# "restored from sites-available" would have taken the whole FastAPI surface
# offline. Edit here, never in sites-enabled.
#
# Architecture:
#   Cloudflare (public HTTPS) → :80 on this host (Nginx) → 127.0.0.1:8765 cas engine
#                                                        → 127.0.0.1:8766 cas-api (FastAPI)
#
# EXPOSURE — the previous comment here was wrong. Nginx does NOT bind loopback
# only: `listen 80;` below binds 0.0.0.0:80 and [::]:80 on every interface.
# Nothing in this file restricts who may connect. The only thing keeping port
# 80 off the open internet is UFW, which allows 80,443/tcp exclusively from
# Cloudflare's published IP ranges. If those UFW rules are ever flushed or the
# ranges go stale, this server is directly reachable -- the config will not
# save you. Verify with:  ufw status | grep -E "^80,443"  and  ss -lnt | grep :80

# WebSocket connection upgrade map (http-level)
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''  close;
}

upstream cas_engine {
    server 127.0.0.1:8765;
    keepalive 4;
    # Development: disable browser caching for static files
}

server {
    listen 80;
    server_name casplatform.com www.casplatform.com;

    # ── Security headers ─────────────────────────────────────────────────────
    # Cloudflare adds X-Frame-Options / HSTS at the edge, but set sane defaults
    add_header X-Content-Type-Options "nosniff"    always;
    add_header X-Frame-Options        "SAMEORIGIN" always;
    add_header Referrer-Policy        "strict-origin-when-cross-origin" always;

    # ── Static frontend ──────────────────────────────────────────────────────
    root  /opt/cas/static;
    index landing.html index.html;

    location / {
        try_files $uri $uri/ =404;
    }

    # ── API reverse proxy → CAS Engine ──────────────────────────────────────

    # FastAPI cas-api (port 8766) — yeni endpoint'ler /api/v2/*
    location /api/v2/ {
        proxy_pass http://127.0.0.1:8766;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header CF-Connecting-IP $http_cf_connecting_ip;
        # WebSocket upgrade (for /api/v2/ws/*)
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        # Long-lived WS connections need a long read timeout (poll interval 15s
        # + heartbeat); REST calls under /api/v2/ are unaffected (they finish fast).
        proxy_read_timeout 3600s;
        proxy_connect_timeout 5s;
    }

    location /api/ {
        # Strip /api prefix before forwarding
        rewrite ^/api/(.*) /$1 break;

        proxy_pass         http://cas_engine;
        proxy_http_version 1.1;
        proxy_set_header   Connection "";      # keep-alive to upstream
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;

        # Engine can be slow on heavy TLE analysis
        proxy_read_timeout    120s;
        proxy_connect_timeout  10s;
        proxy_send_timeout     30s;

        # Buffer responses in memory — avoids half-written JSON to client
        proxy_buffering    on;
        proxy_buffer_size  16k;
        proxy_buffers      8 16k;
    }
location = /sitemap.xml {
    root /opt/cas/static;
    add_header Cache-Control "public, max-age=3600";
}
    # ── Health check (Cloudflare monitors this) ──────────────────────────────
    location = /catalog.html {
        add_header Cache-Control "no-cache, no-store, must-revalidate" always;
        add_header Pragma "no-cache" always;
        add_header Expires "0" always;
        try_files $uri =404;
    }

    location /stats/ {
        proxy_pass              http://cas_engine;
        proxy_buffering         off;
        proxy_read_timeout      60s;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
    }

    location /catalog/ {
        proxy_pass              http://cas_engine;
        proxy_buffering         off;
        proxy_read_timeout      120s;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
    }

    location /tle/ {
        rewrite ^/tle/(.*) /tle/$1 break;
        proxy_pass http://cas_engine;
        proxy_read_timeout 30s;
    }

    location /history {
        rewrite ^/history(.*) /history$1 break;
        proxy_pass http://cas_engine;
        proxy_read_timeout 30s;
    }

    location /health {
        proxy_pass http://cas_engine/health;
        proxy_read_timeout 5s;
        access_log off;
    }

    # ── Allow .well-known (security.txt, ACME) before the dotfile block ──────
    location ^~ /.well-known/ {
        root /opt/cas/static;
        add_header Cache-Control "public, max-age=3600";
        default_type text/plain;
    }

    # ── Block backup / temporary / source files (never serve these) ──────────
    # Two patterns: extension at end (file.html.bak) and mid-name (file.html.bak.20260411)
    location ~* \.(bak|backup|old|orig|save|swp|swo|tmp|dump|sql|log|py|sh|ini|conf|env|dist)$ {
        deny all;
        return 404;
    }
    location ~* \.(bak|backup|old|orig|save|broken|pre-restore)[._-] {
        deny all;
        return 404;
    }

    # ── Block direct access to .env and internal paths ───────────────────────
    location ~ /\. {
        deny all;
        return 404;
    }

    # ── Gzip ─────────────────────────────────────────────────────────────────
    gzip            on;
    gzip_types      application/json text/plain text/css application/javascript;
    gzip_min_length 512;
    # Development: disable browser caching for static files
}
